//1 dan 30 gacha hisoblaydigan juft sonlarni hisoblash
//with function

#include <stdio.h>
#include <stdbool.h>

int even(int num)
{
    if (num % 2 == 0)
    {
        int
        for (int i = 1; i < num; i++);
        printf("%d\n", i);
    }
    else
    {
        return 0;
    }
}

int main()
{
    int num = 30, result;
    result = even(num);
    
    printf("even numbers are: %d\n", result);
    return 0;
}